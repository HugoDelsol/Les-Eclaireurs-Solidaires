<link rel="stylesheet" href="/style/account/styleSideNav.css">
<link rel="stylesheet" href="/style/account/styleListMissionUser.css">
<%- include('../partials/_head') %>

    <main>
        <div class="container" style='display: flex;'>
            <%- include('../partials/_sideNavUser') %>
                <section class="blocListMission">

                    <div class="titleAndCategorySearch">
                        <div>
                            <h1>Mission disponible :</h1>
                            <div class="line"></div>
                        </div>

                        <select name="category" id="">
                            <% for (let category of categoriesMission) { %>
                                <option value="<%= category.categoryId %>">
                                    <%= category.categoryName %>
                                </option>
                                <% } %>
                        </select>
                        <select name="category" id="">
                            <% for (let region of regions) { %>
                                <option value="<%= region.id_region %>">
                                    <%= region.region_name %>
                                </option>
                                <% } %>
                        </select>
                        <a href="/searchByCategories" class="btnSecondary btnHover">Rechercher ✅</a>
                    </div>
                    <table>
                        <thead>
                            <tr>
                                <th>MISSION</th>
                                <th>DATE</th>
                                <th>CATEGORIES</th>
                                <th>LIEUX</th>
                                <th>DETAILS</th>
                                <th>S'INSCRIRE</th>
                            </tr>
                        </thead>
                        <tbody class="fixMax">
                            <% for (let m of missions) { %>
                                <tr>
                                    <td>
                                        <p>
                                            <%= m.mission_title %>
                                        </p>
                                    </td>
                                    <td class="color">
                                        <%= m.mission_date %>
                                    </td>
                                    <td>
                                        <%= m.mission_category_name %>
                                    </td>
                                    <td class="color">
                                        <%= m.city_name %>
                                    </td>
                                    <td> <a href="/idMission">Voir d’avantage</a></td>
                                    <td>
                                        Je participe
                                        <input type="checkbox">
                                    </td>
                                </tr>
                                <% } %>
                        </tbody>
                    </table>
                </section>
        </div>
    </main>

    </body>

    </html>